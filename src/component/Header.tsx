import React from "react";
import { HeaderStyled, HeaderContainer } from "./HeaderStyled";

const Header: React.FC = () => {
  return (
    <HeaderContainer>
      <HeaderStyled>
        <p>Header...</p>
      </HeaderStyled>
    </HeaderContainer>
  );
};

export default Header;
