import React from 'react';
import PropTypes from 'prop-types';

const HomeStyle = {
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
};

const Home = () => (
  <div style={HomeStyle}>
    <h1>
      Welcome!
    </h1>
  </div>
);

export default Home;
