import React from 'react';
// import PropTypes from 'prop-types';

const HomeViewStyle = {
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
};

const HomeView = () => (
  // eslint-disable-next-line react/jsx-filename-extension
  <div style={HomeViewStyle}>Welcome</div>
);

HomeView.propTypes = {};

export default HomeView;
