import React from "react";

const HomeStyle = {
  display: "flex",
  alignItems: "center",
  justifyContent: "center"
};

const HomePage = () => (
  <div style={HomeStyle}>
    <h3>Welcome ...</h3>
  </div>
);

HomePage.propTypes = {};

export default HomePage;
